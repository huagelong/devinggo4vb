# Admin UI 重构按周执行 ToDo

更新时间：2026-03-25  
关联方案：`docs/PLAN-AdminUI-Refactor.md`

---

## 执行进度（2026-03-26）

- 已完成：Week 1 除 `user` 页冒烟测试外全部项（`typecheck` 已通过）。
- 已完成：Week 2 5 项 ToDo 全部落地（`user + post` 双页已接入统一模式）。
- 已补充文档：`docs/ADMIN-Common-CRUD-Guide.md`。
- 当前阻塞：`user` 页冒烟测试尚未在联调环境执行完成。

---

## 执行边界（固定）

- 不迁移页面：`system/module`、`system/code`。
- 不清理模块能力与权限码（例如 `system:systemModules:*` 继续保留）。
- 目标目录：`admin-ui/apps/backend/src/views/system`、`src/api/system`、`src/composables`。

---

## Week 1：基线修正与模板页定版

目标：把 `system/user` 固化为标准模板页，解决后续复制时会反复踩坑的问题。

ToDo：

- [x] 修复 `system/user` 分页参数统一为 `page/pageSize`。
- [x] 补齐 `system/user` 与旧版对齐项：
- [x] 超级管理员保护逻辑（禁删/禁用等）。
- [x] 设置首页能力。
- [x] 导入/导出能力。
- [x] 编辑回填一致性（必要时补读详情）。
- [x] 产出页面模板说明（`index + modal + schemas + api` 标准结构）。
- [ ] 完成一次 `typecheck` 与 `user` 页冒烟测试。（已完成 `typecheck`，冒烟测试待联调环境执行）

本周输出：

- 代码：`system/user` 标准化。
- 文档：页面模板开发说明（可放 `docs`）。

验收门槛：

- `user` 页核心链路可用：查询/分页/新增/编辑/删除/回收站/权限按钮。

---

## Week 2：公共能力抽取（轻封装）

目标：提效但不走黑盒。

ToDo：

- [x] 新增 `use-crud-page.ts`（查询、分页、loading、刷新、批量选中）。
- [x] 新增 `use-dict-options.ts`（字典请求、缓存、options 转换）。
- [x] 新增通用工具栏组件（刷新、回收站切换、列显隐）。
- [x] 在 `user` 页接入公共能力，完成第一轮去重。
- [x] 选 1 个占位页试接入模板（建议 `post`）。

本周输出：

- 代码：`composables/crud/*` + 通用组件初版。
- 文档：公共能力使用说明与示例。

验收门槛：

- `user + post` 至少 2 页使用统一模式。

---

## Week 3：P0 页面迁移（第一批）

目标：先打通 RBAC 主链路的 3 个核心页面。

ToDo：

- [ ] 迁移 `role` 页面（列表、增改删、状态、回收站、权限按钮）。
- [ ] 迁移 `menu` 页面（树结构、增改删、排序、状态）。
- [ ] 迁移 `dept` 页面（树结构、增改删、状态、关联能力）。
- [ ] 补齐对应 `api/system/*.ts` 与最小类型定义。
- [ ] 联调动态菜单与按钮权限控制。

本周输出：

- 代码：`role/menu/dept` 页面可用。
- 代码：对应 API 与类型文件。

验收门槛：

- P0 第一批页面可进入联调环境使用。

---

## Week 4：P0 页面迁移（第二批）+ P0 收口

目标：完成 P0 页面并做第一轮回归。

ToDo：

- [ ] 迁移 `dict` 页面（字典类型 + 字典数据联动）。
- [ ] 迁移 `post` 页面。
- [ ] P0 全量回归：`user/role/menu/dept/dict/post`。
- [ ] 统一整理 P0 页面交互差异（空状态、错误提示、按钮位置）。

本周输出：

- 代码：P0 页面全量完成。
- 文档：P0 功能对齐清单与差异说明。

验收门槛：

- P0 页面满足日常可用标准，可支持测试同学稳定回归。

---

## Week 5：P1 页面迁移（第一批）

目标：推进系统配置与运维页面。

ToDo：

- [ ] 迁移 `config` 页面。
- [ ] 迁移 `crontab` 页面（含日志列表）。
- [ ] 迁移 `notice` 页面。
- [ ] 补齐相关 API、类型和弹窗表单。

本周输出：

- 代码：`config/crontab/notice` 页面。
- 文档：P1 第一批接口映射表。

验收门槛：

- 三个页面核心 CRUD 与筛选分页稳定可用。

---

## Week 6：P1 页面迁移（第二批）

目标：完成运维与资源相关页面。

ToDo：

- [ ] 迁移 `attachment` 页面（上传、预览、删除、回收站）。
- [ ] 迁移 `monitor/onlineUser` 页面。
- [ ] 迁移 `monitor/cache` 页面。
- [ ] 整理上传与下载相关通用处理（错误提示、loading、文件名）。

本周输出：

- 代码：P1 页面全量完成。
- 文档：P1 回归清单。

验收门槛：

- P1 页面全部可用，常见异常路径有一致提示。

---

## Week 7：P2 页面迁移（第一批）

目标：补齐业务扩展页。

ToDo：

- [ ] 迁移 `api` 页面。
- [ ] 迁移 `apiGroup` 页面。
- [ ] 迁移 `app` 页面。

本周输出：

- 代码：`api/apiGroup/app` 页面可用。

验收门槛：

- P2 第一批页面完成联调并通过基本回归。

---

## Week 8：P2 页面迁移（第二批）+ 全量联调

目标：完成收口页面并进行跨页面联调。

ToDo：

- [ ] 迁移 `appGroup` 页面。
- [ ] 迁移 `systemModules` 页面。
- [ ] 全量系统页联调与缺陷修复。
- [ ] 增加动态菜单路径容错，避免返回无对应页面时崩溃。

本周输出：

- 代码：P2 页面全量完成。
- 文档：全量联调问题清单与修复记录。

验收门槛：

- 重构范围内页面全部可用，主流程无阻塞缺陷。

---

## Week 9：提效与稳定化

目标：把“持续开发能力”补齐，降低后续新增页面成本。

ToDo：

- [ ] 新增 `gen:crud` 脚手架（生成 `api + model + schemas + index + modal`）。
- [ ] 增加页面 PR 检查清单（类型、权限、错误处理、交互一致性）。
- [ ] 补齐《新增页面开发指南》与《重构完成报告》。
- [ ] 做一次性能与体积检查（按需优化）。

本周输出：

- 工具：CRUD 页面生成脚手架。
- 文档：开发指南 + 完成报告。

验收门槛：

- 新增一个标准 CRUD 页面可在 0.5~1 天内交付首版。

---

## 周例会检查模板（建议）

每周固定检查：

- [ ] 本周承诺项完成率（完成/延期/阻塞）。
- [ ] 新增阻塞问题与责任人。
- [ ] 下周任务是否可执行（依赖是否齐备）。
- [ ] 风险变化（接口变更、权限变更、设计变更）。

---

## 里程碑定义

- 里程碑 M1：Week 2 结束，模板与公共能力可复用。  
- 里程碑 M2：Week 4 结束，P0 页面闭环。  
- 里程碑 M3：Week 6 结束，P1 页面闭环。  
- 里程碑 M4：Week 8 结束，范围内页面全部完成。  
- 里程碑 M5：Week 9 结束，进入稳态开发模式。  
